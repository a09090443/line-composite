spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: core
  cloud:
    config:
      profile: dev
      uri: http://127.0.0.1:8000

server:
  port: 8080

application:
  cache:
    enable: true
